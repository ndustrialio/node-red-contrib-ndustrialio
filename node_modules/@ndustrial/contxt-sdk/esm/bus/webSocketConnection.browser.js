function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

/**
 * Module that wraps the websocket connection to the message bus to provide the
 * developer with a specific set of functionality. This is for browser
 * environments. Documentation for Node environments is found under
 * `WebSocketConnection`.
 *
 * @alias BrowserWebSocketConnection
 */
var WebSocketConnection =
/**
 * @alias BrowserWebSocketConnection
 */
function WebSocketConnection(webSocket, organizationId) {
  _classCallCheck(this, WebSocketConnection);

  throw new Error('The Message Bus is not currently supported in browser environments');
};

export default WebSocketConnection;
//# sourceMappingURL=webSocketConnection.browser.js.map
